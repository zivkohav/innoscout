// src/services/refinementService.ts

// Simple placeholder so the app builds and refinement buttons still work.
// We can later move evaluation refinement logic to a backend API route.

export const formalizeRefinementRule = async (rawInput: string): Promise<string> => {
  return rawInput.trim();
};